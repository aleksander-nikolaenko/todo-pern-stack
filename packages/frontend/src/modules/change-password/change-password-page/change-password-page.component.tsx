import React from 'react';
import * as Styled from './change-password-page.styled';

export const PasswordPageComponent = () => (
  <Styled.PageContainer>Change password page</Styled.PageContainer>
);
